#ifndef INPUT_H
#define INPUT_H
#include <iostream>
#include <string>
#include <sstream>

/** Asks the user to enter a number between min and max, inclusive */
int userInput(int min, int max);

#endif
